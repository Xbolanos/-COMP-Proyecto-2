//maze.walker=0;
	struct Object getABCD(struct Object object){
		Framebuffer = (struct Color **)malloc(Vres * hola);

		Framebuffer = (struct Color **)malloc(Vres * sizeof(struct Color*));

		return object; //Probar esto, sino regresar un array con ABCD
	}
